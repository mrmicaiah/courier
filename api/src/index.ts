/**
 * Courier - Email Marketing Platform
 * Main entry point (TypeScript version with MCP SDK)
 * 
 * This file will replace index.js when we switch over
 */

import { CourierMCP } from './mcp';

// Re-export the MCP class for Durable Objects
export { CourierMCP };

// For now, this is just a placeholder
// The actual switch will happen when we update wrangler.toml
// to use this file as the entry point
